worker_processes auto;

events {
    worker_connections 1024;
}

http {
    server {
        listen 80;
        server_name _;

        location /prometheus/ {
            proxy_pass           http://prometheus:9090/;
            proxy_set_header     Host              $host;
            proxy_set_header     X-Real-IP         $remote_addr;
            proxy_set_header     X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header     X-Forwarded-Proto $scheme;
        }

        location /grafana/ {
            proxy_pass           http://grafana:3000/;
            proxy_set_header     Host              $host;
            proxy_set_header     X-Real-IP         $remote_addr;
            proxy_set_header     X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header     X-Forwarded-Proto $scheme;
        }

        location /alertmanager/ {
            proxy_pass           http://alertmanager:9093/;
            proxy_set_header     Host              $host;
            proxy_set_header     X-Real-IP         $remote_addr;
            proxy_set_header     X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header     X-Forwarded-Proto $scheme;
        }

        location /alert-receiver/ {
            proxy_pass           http://alert_receiver:5001/;
            proxy_set_header     Host              $host;
            proxy_set_header     X-Real-IP         $remote_addr;
            proxy_set_header     X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header     X-Forwarded-Proto $scheme;
        }

        location /flask/ {
            proxy_pass           http://flask_app:8000/;
            proxy_set_header     Host              $host;
            proxy_set_header     X-Real-IP         $remote_addr;
            proxy_set_header     X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header     X-Forwarded-Proto $scheme;
        }

        location / {
            root  /usr/share/nginx/html;
            index index.html;
        }
    }

    # ========== BasicAuth 加固版（报告演示用） ==========
    # 取消下方注释即可启用 BasicAuth，展示安全加固效果
    # 启用后：所有服务需输入 admin/monitoring2025 才能访问
    #
    # server {
    #     listen 8081;
    #     server_name _;
    #
    #     location /prometheus/ {
    #         auth_basic           "Prometheus Monitoring";
    #         auth_basic_user_file /etc/nginx/.htpasswd;
    #         proxy_pass           http://prometheus:9090/;
    #         proxy_set_header     Host              $host;
    #         proxy_set_header     X-Real-IP         $remote_addr;
    #         proxy_set_header     X-Forwarded-For   $proxy_add_x_forwarded_for;
    #         proxy_set_header     X-Forwarded-Proto $scheme;
    #     }
    #
    #     location /grafana/ {
    #         auth_basic           "Grafana Dashboard";
    #         auth_basic_user_file /etc/nginx/.htpasswd;
    #         proxy_pass           http://grafana:3000/;
    #         proxy_set_header     Host              $host;
    #         proxy_set_header     X-Real-IP         $remote_addr;
    #         proxy_set_header     X-Forwarded-For   $proxy_add_x_forwarded_for;
    #         proxy_set_header     X-Forwarded-Proto $scheme;
    #     }
    #
    #     location /alertmanager/ {
    #         auth_basic           "Alertmanager";
    #         auth_basic_user_file /etc/nginx/.htpasswd;
    #         proxy_pass           http://alertmanager:9093/;
    #         proxy_set_header     Host              $host;
    #         proxy_set_header     X-Real-IP         $remote_addr;
    #         proxy_set_header     X-Forwarded-For   $proxy_add_x_forwarded_for;
    #         proxy_set_header     X-Forwarded-Proto $scheme;
    #     }
    #
    #     location /alert-receiver/ {
    #         auth_basic           "Alert Receiver";
    #         auth_basic_user_file /etc/nginx/.htpasswd;
    #         proxy_pass           http://alert_receiver:5001/;
    #         proxy_set_header     Host              $host;
    #         proxy_set_header     X-Real-IP         $remote_addr;
    #         proxy_set_header     X-Forwarded-For   $proxy_add_x_forwarded_for;
    #         proxy_set_header     X-Forwarded-Proto $scheme;
    #     }
    #
    #     location /flask/ {
    #         auth_basic           "Flask App";
    #         auth_basic_user_file /etc/nginx/.htpasswd;
    #         proxy_pass           http://flask_app:8000/;
    #         proxy_set_header     Host              $host;
    #         proxy_set_header     X-Real-IP         $remote_addr;
    #         proxy_set_header     X-Forwarded-For   $proxy_add_x_forwarded_for;
    #         proxy_set_header     X-Forwarded-Proto $scheme;
    #     }
    #
    #     location / {
    #         return 200 'BasicAuth enabled - all services require authentication';
    #         add_header Content-Type text/plain;
    #     }
    # }
}
